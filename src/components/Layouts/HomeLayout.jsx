import React from 'react';
import Navbar from '../shared/Navbar';

const HomeLayout = () => {
    return (
        <section>
            <Navbar></Navbar>
        </section>
    );
};

export default HomeLayout;